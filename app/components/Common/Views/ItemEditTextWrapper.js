import styled from 'styled-components/native';

const ItemEditTextWrapper = styled.TouchableOpacity`
    flex: 0.85;
    flex-direction: row;
    padding: 25px 20px;
`;

export default ItemEditTextWrapper;