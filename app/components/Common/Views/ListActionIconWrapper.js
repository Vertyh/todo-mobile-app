import styled from 'styled-components/native';

const ListActionIconWrapper = styled.TouchableOpacity`
    flex: 0.15;
    align-items: center;
    justify-content: center;
    padding-right: 25;
`;

export default ListActionIconWrapper;